# BACKEND secrets (copy to server/.env). This file is read ONLY by server/index.js
# and never reaches the browser. It is git-ignored.
ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-opus-5
PORT=8787
# Comma-separated origins allowed to call the API (your deployed frontend URL in production)
ALLOWED_ORIGINS=http://localhost:5173
